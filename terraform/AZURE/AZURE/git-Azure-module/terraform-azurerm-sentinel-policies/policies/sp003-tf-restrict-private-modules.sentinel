import "tfconfig"

is_private_module = rule {
    all tfconfig.modules as name, m {
        m.source matches "pwc.com" or m.source matches "pwcinternal.com"
    }
}

main = rule {
    is_private_module
}
