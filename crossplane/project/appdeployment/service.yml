apiVersion: v1
kind: Service
metadata:
  name: awscommunityday
spec:
  ports:
    - port: 8080
      targetPort: 8090
  # type: NodePort  # Change service type to NodePort
  selector:
    app: awscommunityday
    branch: main